On my ubuntu server i have only nodeJS installed. I want you to build a single "server.js" file that when runs creates a folder along side it called "Server" which contains all other server related data in subfolders. I want zero npm dependencies and no CDN or external assets.  When run it listens on port 8080 as a web server. If you browse to the endpoint "/admin" you initially have to create a username/password and setup TOTP. After that initial setup it instead prompts to sign in with those. There will only be 1 admin account stored. Once logged in you see the main dashboard where you can see all of the hardware and network statistics of the server, to give you a complete view of the servers health over the last hour. This also has maintenance buttons as well for things like updating the server and things like that. You can also click to another tab that shows the full current server.js wtih syntax highlighting and 4 space tab indentation and you can make any changes you want and then click "Update" it checks the syntax and creates a backup of the current version and then reboots the server using the new server.js. If there are any issues it reverts to the most recent backup letting you know the issue. There is another tab which is a full file manager of the entire server defaulting to the Server folder. In there you can drag and drop files or folders in, you can download files and folders as zips aswell, you can also edit files with html files having syntax highlighting for html. All changes update instantly. I also want teh option to be able to share files or folders using a custom endpoint that i can share which can optionally expire and change permissions as i could share a file allowing someone else to edit without needing to log in. I want an easy way to track all my shares though. Another tab is called Caddy, as the server.js also installs and setups up Caddy aswell and on the webpage you can see the status of docker and caddy and also edit the CaddyFile which by default redirects an example domain to the main localhost:8080 webpage and applies SSL like normal. Both docker, caddy and this node server all need to auto run when the server reboots. Another Tab is called API and allows you to easily create custom endpoints and change if its scheduled to run at certain days or times or when things happen on the portal like someone signing in or when the endpoint is triggered via POST or GET or any others and there is a section where you can enter JS that runs on it being triggered. I alos want there to be a folder created in Server called "public" and files in here are provided to the user via endpoint identically to how github pages operates, for example "/" provides "/public/index.html" if it exists, if not it errors, or endpoint "/testFolder/test.txt" checks "/public/testFolder/test.txt" file and provides it. "/admin" and api endpoints take priority over the public endpoints though. I also want a web terminal to save me having to keep SSH'ing into my server.  Privileges: Run as root Update reboot: Fast systemd restart, not full OS reboot Web terminal: Full real bash/pty shell Secret storage: Encrypted at rest, machine-bound key file (no manual passphrase entry needed at boot) HTTPS on :8080: HTTP-only; publicly reachable initially, can be firewalled to localhost once Caddy is set up Share links: Random unguessable token only, no password layer, with expiry + permission flags API JS execution: Fully unrestricted, no sandboxing Stats sampling: Every 60 seconds, 60 points/hour, persisted across restarts Backups: Last 10 rolling versions for both server.js and Caddyfile, with syntax-check + auto-rollback Docker: Dropped entirely — Caddy runs natively via systemd.

Instead of creating the whole server.js all at once, generate it in stages and prompt me to let you continue as doing it all at once is causing issues.
